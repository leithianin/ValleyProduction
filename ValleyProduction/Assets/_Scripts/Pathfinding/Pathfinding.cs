using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pathfinding : VLY_Singleton<Pathfinding>
{


   /* private void StartFindPath(PathNode startNode, PathNode targetNode, BuildTypes landmarkTarget)
    {
        StartCoroutine(FindPath(startNode, targetNode, landmarkTarget));
    }

    IEnumerator FindPath(PathNode startNode, PathNode targetNode, BuildTypes landmarkTarget)
    {
        PathNode[] nodesToTakes = new PathNode[0];
        bool pathSuccess = false;

    
    }*/


}
